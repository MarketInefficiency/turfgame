# Fail fast with a clear message if installed under an unsupported Node major.
# The Colyseus uWebSockets.js transport only ships prebuilt binaries for Node 18-22
# (see package.json "engines" and .nvmrc). Without this, a wrong Node version surfaces
# as a cryptic native-module error at server boot instead of at install time.
engine-strict=true
